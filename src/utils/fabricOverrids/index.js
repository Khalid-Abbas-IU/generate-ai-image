import {fabric} from "fabric";
fabric.Object.prototype.set({
    transparentCorners: false,
    // hasBorders:false,
    // hasControls:false,
    cornerColor: '#333333',
    borderColor: '#333333',
    cornerSize: 8,
    // borderScaleFactor:5, //border stroke size
    cornerStyle: 'circle',
});